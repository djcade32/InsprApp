const primaryFontFamily = 'Lato';

const size = {
  sm: 18,
  md: 20,
  lg: 24,
  xlg: 32,
};

const weight = {
  light: '300',
  bold: 'bold',
  medium: '500',
};

export default {size, weight, primaryFontFamily};
